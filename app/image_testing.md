## TEST AGENT PROMPT – IMAGE INTEGRATION RULES ##
- Use base64-encoded images (JPEG/PNG/WEBP only).
- Never use blank / solid-color images. Use real photos of product labels or objects.
- Resize large payloads to reasonable bounds.
- If GIF/APNG, extract first frame.
- Re-detect MIME after transformations.
